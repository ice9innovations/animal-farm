# JetPack 6 / CUDA 12.6 application dependencies.
#
# NudeNet and ONNX Runtime are installed separately by install_jetson.sh:
# NudeNet declares a dependency on the CPU "onnxruntime" distribution, while
# Jetson needs the CUDA-enabled "onnxruntime-gpu" distribution instead.
numpy<2
opencv-python-headless<4.12
flask>=2.0.0
flask-cors>=3.0.0
requests>=2.25.0
python-dotenv>=0.19.0
Pillow>=8.3.0
# Dependencies declared by onnxruntime-gpu. Install these from PyPI before
# installing the Jetson wheel with --no-deps.
coloredlogs
flatbuffers
packaging
protobuf
sympy
