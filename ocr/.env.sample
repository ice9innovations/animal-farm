# EasyOCR GPU Text Extraction Service Configuration

# Service Settings
PORT=7775
PRIVATE=false

TIMEOUT=30

# Configuration Updates (GitHub-first pattern)
AUTO_UPDATE=true

# Required by default. The service exits at startup if CUDA PyTorch is unavailable.
OCR_REQUIRE_GPU=true
