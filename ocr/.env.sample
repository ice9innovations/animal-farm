# RapidOCR Text Extraction Service Configuration

# Service Settings
PORT=7775
PRIVATE=false

TIMEOUT=30

# Configuration Updates (GitHub-first pattern)
AUTO_UPDATE=true

# Optional. CPU is the portable default for desktop and Jetson.
# Set true only when your onnxruntime installation exposes CUDAExecutionProvider.
OCR_USE_CUDA=false
